CREATE OR REPLACE PACKAGE PKG_LIMITECREDITO IS
  TYPE TR_DADOS_PARAMETROS IS RECORD(
    UTILIZALIMITECREDITOCPF  BOOLEAN,
    SOMACREDITOCLIPRINCIPAL  BOOLEAN,
    PEREXCEDELIMCRED         NUMBER,
    USABNFLIMITECREDITO      BOOLEAN,
    ACEITAVENDABLOQ          BOOLEAN,
    VERIFICALIMCREDCODCOBD   BOOLEAN,
    CHECARDIASUTEISTITPGNLIB BOOLEAN);

  PARAMETROS T_PC_PARAMETROS := T_PC_PARAMETROS('PKG_LIMITECREDITO');

  FUNCTION RETORNA_LIMITE_CRED_DISPONIVEL(CODIGOCLIENTE          NUMBER,
                                          UTILIZALIMITEEXTRA     BOOLEAN DEFAULT TRUE,
                                          UTILIZALIMITEPARCEIROS BOOLEAN DEFAULT FALSE,
                                          NUMEROPEDIDOATUAL      NUMBER DEFAULT 0)
    RETURN NUMBER;

  FUNCTION RETORNA_LIMITE_CREDITO_CLIENTE(CODIGOCLIENTE NUMBER) RETURN NUMBER;

  FUNCTION RETORNA_CONTAS_RECEBER_ABERTO(CODIGOCLIENTE NUMBER) RETURN NUMBER;

  FUNCTION RETORNA_LIMITE_CREDITO_EXTRA(CODIGOCLIENTE NUMBER) RETURN NUMBER;

  FUNCTION RETORNA_PEDIDOS_FATURAR(CODIGOCLIENTE     NUMBER,
                                   NUMEROPEDIDOATUAL NUMBER DEFAULT 0)
    RETURN NUMBER;

  FUNCTION RETORNA_TOTAL_CHEQUE(CODIGOCLIENTE NUMBER) RETURN NUMBER;

  FUNCTION RETORNA_DIFERENCA_TIT_TERCEIRO(CODIGOCLIENTE NUMBER) RETURN NUMBER;

  FUNCTION RETORNA_LIMITE_CREDITO_SAZONAL(CODIGOCLIENTE NUMBER,
                                          CODIGORCA     NUMBER) RETURN NUMBER;

END;
