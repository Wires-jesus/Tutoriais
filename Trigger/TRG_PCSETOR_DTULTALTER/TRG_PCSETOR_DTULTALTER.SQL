CREATE OR REPLACE TRIGGER TRG_PCSETOR_DTULTALTER
   BEFORE INSERT OR UPDATE ON PCSETOR  REFERENCING OLD AS OLD NEW AS NEW FOR EACH ROW
DECLARE
BEGIN
   IF INSERTING OR UPDATING THEN
      :NEW.DTULTALTER := SYSDATE;
   END IF;
END;
