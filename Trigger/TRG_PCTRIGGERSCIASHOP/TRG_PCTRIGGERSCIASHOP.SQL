CREATE OR REPLACE TRIGGER TRG_PCTRIGGERSCIASHOP
BEFORE INSERT ON PCTRIGGERSCIASHOP
REFERENCING NEW AS NEW OLD AS OLD
FOR EACH ROW
WHEN (NEW.ID IS NULL)
BEGIN
 SELECT DFSEQ_PCTRIGGERSCIASHOP.NEXTVAL
 INTO   :NEW.ID
 FROM   DUAL;
END;
